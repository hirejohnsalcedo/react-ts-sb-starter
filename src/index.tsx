import React from "react";
import ReactDOM from "react-dom";

const App = () => {
  return <span>it do be like that</span>;
};

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

export default App;
